{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "this is a node server for ichat",
  "main": "index.js",
  "scripts": {
    "start": "node ./index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && npm install socket.io "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.1.0",
    "socket.io": "^4.7.4"
  }
}
